# TypeScript build
api/dist/
api/.tsbuildinfo

# Prisma / SQLite dev DBs (keep schema & migrations tracked)
api/.prisma/
api/prisma/dev.db*
api/prisma/*.sqlite*
api/prisma/*.sqlite-journal

# Env & secrets (commit .env.sample only)
api/.env
api/.env.*

# Replit
.replit
replit.nix

# macOS & editor cruft
.DS_Store
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Zips / local exports
*.zip
